import { Form } from "../widgetReactComponents";

const rootDefault = {
    component: Form,
    mode: "editorHolder",
    propsMixinList: ["layout"],
};

export default {
    default: rootDefault,
};
